function reverce(arr){
    let reslt=[];
    for(let i=arr.length-1;i>=0;i--){
    reslt.push(arr[i]);
    }
    return reslt;
}
console.log(reverce([1,2,3,4]))
function reverce(arr){
    reslt=arr.reverse();
    return reslt

}
console.log(reverce([1,2,3,4]))
